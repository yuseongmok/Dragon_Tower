# 드래곤 타워 — 첫 전투 프로토타입

**현재 실행 흐름은 알 → 부화 → 로비입니다.** 최신 사용법은 [알과 로비 시작 안내](알과%20로비%20시작%20안내.md)를 보세요. 아래 내용은 초기 전투 프로토타입 설명이며, 엠버 고정·즉시 전투 시작 방식은 더 이상 사용하지 않습니다.

**픽셀아트 업데이트:** 현재는 엠버·루나·제피르·블랜디·볼트·옥타·노바·단테와 1~10층 몬스터·타워 배경이 적용됩니다. 최신 적용 방법과 설정은 [픽셀아트 적용 안내](픽셀아트%20적용%20안내.md)를 참고하세요. 아래의 랜덤 선택·임시 도형 설명은 초기 버전에 관한 내용입니다.

## 지금 준비된 것

Unity **6000.3.10f1**용 프로젝트입니다. 이 PC에는 해당 버전과 Web 빌드 모듈이 설치되어 있습니다. 다른 프로젝트를 수정하지 않고 새 폴더에 만들었습니다.

프로젝트에는 전투 씬과 드래곤 8종의 데이터가 준비되어 있습니다. 각 드래곤에는 전용 픽셀 이미지와 스킬이 연결되며, 이미지가 없는 새 데이터는 실행 목록에서 제외됩니다.

- 480 × 850 기준 세로 화면. 화면 크기에 맞춰 축소·확대하며 여백을 유지합니다.
- 위쪽 적 드래곤, 아래쪽 플레이어 드래곤과 각각의 HP 표시.
- 하단 가운데 **공격**, 왼쪽 **스킬**, 오른쪽 **회피** 버튼.
- 기본 공격 9 피해, **0.3초 쿨타임**. 한 번 누르면 한 번 공격합니다.
- 적은 2.4초마다 18 피해를 줍니다. 공격 0.85초 전부터 게이지가 찹니다.
- 회피는 **0.42초 무적**, **1.4초 쿨타임**. 게이지가 거의 찼을 때 누릅니다.
- 승리·패배 표시와 재도전. 전투가 끝나면 추가 공격을 막습니다.
- 시작과 재도전 때 3종 중 무작위 드래곤을 고릅니다. 현재는 알 부화 과정 없이 전투부터 시작합니다.

## 1. 프로젝트 열기

1. 이 폴더의 **Unity로 열기.cmd**를 더블클릭합니다. 또는 Unity Hub → **Projects → Add → Add project from disk**에서 이 `DragonTower` 폴더를 선택합니다. `Assets` 폴더가 아니라 그 상위 폴더입니다.
2. 에디터 버전은 **6000.3.10f1**을 선택합니다.
3. 첫 실행의 파일 가져오기와 스크립트 컴파일이 끝날 때까지 기다립니다.
4. 자동 생성이 완료되면 `Assets/Scenes/Battle.unity`가 열립니다. Hierarchy에는 `Camera`, `Battle UI`, `Battle Controller`, `EventSystem`이 나타납니다.
5. 자동 생성되지 않고 Console에 오류가 없다면 상단 **Dragon Tower → Create prototype scene**을 누릅니다. 이 메뉴는 전투 씬을 재생성하므로 나중에 씬 배치를 수정한 뒤에는 함부로 다시 실행하지 마세요. 기존 드래곤 데이터는 유지합니다.

## 2. 첫 플레이

1. 위쪽 **Game** 탭을 누릅니다.
2. Game 탭의 화면 비율 메뉴에서 `+`를 눌러 **Fixed Resolution / Width 480 / Height 850**을 추가합니다. 이름은 `Mobile Portrait`로 지정하면 됩니다.
3. 상단 가운데 **▶ Play**를 누릅니다.
4. 가운데 공격 버튼을 여러 번 눌러 적 HP가 줄어드는지 확인합니다. PC에서는 마우스로 터치 동작을 대신합니다.
5. 왼쪽 스킬 버튼을 누릅니다. 피해가 크게 들어가고 남은 쿨타임이 표시됩니다.
6. 주황색 게이지가 가득 차기 **0.4초 이내**에 오른쪽 회피 버튼을 누릅니다. 성공하면 `회피 성공!`이 표시되고 HP가 줄지 않습니다.
7. 아무것도 누르지 않으면 적 공격을 맞아 패배합니다. 공격과 스킬을 사용하면 승리할 수 있습니다.
8. 결과 화면의 재도전 버튼을 누르면 HP와 쿨타임이 초기화됩니다. 무작위 선택이므로 같은 드래곤이 다시 나올 수도 있습니다.
9. 테스트가 끝나면 **▶ Play**를 다시 눌러 종료합니다.

**Play 중에 바꾼 Inspector 값은 플레이를 종료하면 원래대로 돌아갈 수 있습니다. 데이터 수정은 Play를 종료한 뒤 하세요.**

## 3. 드래곤과 스킬 값 바꾸기

처음 열기가 끝난 뒤 Project 창의 `Assets/Data`에서 데이터 파일을 선택합니다. 오른쪽 Inspector에 값이 나타납니다.

| 데이터 | 드래곤 | HP | 스킬 | 피해 | 쿨타임 |
|---|---|---:|---|---:|---:|
| Dragon0 / Skill0 | 엠버 · FIRE | 100 | 화염 폭발 | 38 | 4초 |
| Dragon1 / Skill1 | 루나 · ICE | 115 | 서리 숨결 | 52 | 5.5초 |
| Dragon2 / Skill2 | 제피르 · WIND | 90 | 질풍 강타 | 24 | 2.5초 |

- `Dragon0` 등의 **Max HP**, **Attack Damage**, **Color**를 바꾸면 다음 전투부터 반영됩니다.
- `Skill0` 등의 **Damage**, **Cooldown**을 바꾸면 스킬 세기를 바꿀 수 있습니다.
- 데이터는 ScriptableObject 에셋입니다. 전투 중 HP와 쿨타임은 별도로 복사해 관리하므로 원본 능력치가 닳거나 바뀌지 않습니다.
- 종을 추가하려면 Project 창에서 우클릭 → **Create → Dragon Tower → Skill / Dragon**을 생성하고 연결합니다. 이어서 Hierarchy의 **Battle Controller → Dragons** 목록에 새 Dragon 에셋을 추가합니다.

## 4. Web 빌드

1. 먼저 Play를 종료합니다.
2. 상단 **Dragon Tower → Build Web**을 누릅니다.
3. 성공하면 이 프로젝트의 `Builds/Web`에 결과가 생성됩니다. 첫 빌드는 시간이 걸릴 수 있습니다.
4. Web 결과는 HTTP 서버에서 열어야 합니다. `index.html` 더블클릭으로는 실행되지 않을 수 있습니다. Unity의 **File → Build Profiles → Web → Switch Profile → Build And Run**을 사용하면 로컬 서버에서 테스트할 수 있습니다. 빌드 씬 목록에 `Assets/Scenes/Battle.unity`가 포함되어 있어야 합니다.

사용자 지정 Web 페이지는 브라우저에서도 세로 비율을 유지합니다. 가로로 기기를 돌리면 화면 자체를 강제로 잠그는 대신 세로 게임 화면을 작게 표시합니다. 브라우저의 실제 방향 잠금은 기기와 전체화면 권한에 따라 제한됩니다.

픽셀아트 업데이트부터 `Dragon Tower → Build Web`은 Gzip 압축과 브라우저 압축 해제 fallback을 적용합니다. 실제 배포 단계에서는 서버의 압축 응답 설정까지 점검해야 합니다.

Unity 6.3의 공식 지원 목록에는 iOS Safari 15 이상과 Android Chrome 58 이상이 포함됩니다. 실제 대상 기기의 최신 브라우저로 터치·화면 잘림·성능을 확인해야 합니다. [Unity 공식 브라우저 호환성 문서](https://docs.unity.com/en-us/engine/6000.3/manual/platform-specific/webgl/intro/browsercompatibility)

## 검증 상태와 현재 제한

- 전투 규칙의 독립 실행 자동 검사 **22개 통과**: 쿨타임, 피해, 회피, 승패, 재시작, 다른 스킬 설정.
- 설치된 Unity 6000.3.10f1의 실제 어셈블리와 UI 소스를 참조한 **전체 프로젝트 C# 컴파일 통과**.
- **픽셀아트 업데이트에서 Unity 렌더링과 Web 빌드, 데스크톱 브라우저 실행을 확인했습니다.** 초기 버전 때의 캐시 접근 제한은 이번 검증용 복사본 실행에서 해결되었습니다. 휴대폰 실기기 테스트는 아직 수행하지 않았습니다.
- 에디터를 정상적으로 열 수 있게 되면 **Dragon Tower → Verify combat rules**로 Unity 안에서도 기본 검사를 실행할 수 있습니다. Console에서 `DRAGON_TOWER_TESTS_PASSED`를 확인합니다.

## 다음 제작 단계

첫 전투가 정상 실행되는 것을 확인한 뒤 **랜덤 알 → 터치 부화 → 드래곤 확정**, 이어서 **층별 적 → 전투 보상 3개 중 선택 → 능력치 성장 → 보스 → 패배 시 새 도전**을 추가할 계획입니다. 현재 버전에는 알·육성·층 이동·저장 기능이 없습니다.

## 글꼴

한글 UI는 Noto Sans KR을 사용합니다. PC에 설치된 가변 글꼴의 굵기를 500으로 고정한 버전이며 전체 한글 글리프를 유지했습니다. SIL Open Font License와 저작권 고지는 `Assets/Fonts/OFL.txt`에 포함되어 있습니다.
