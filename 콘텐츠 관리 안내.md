# 드래곤 타워 콘텐츠 관리 사용법

코드를 열지 않고 드래곤·몬스터·스킬·아이템·증강을 추가하고 밸런스를 수정하기 위한 도구입니다.

## Unity에서 여는 방법

1. Unity가 스크립트 가져오기를 마칠 때까지 기다립니다.
2. 위쪽 메뉴에서 **Dragon Tower → 콘텐츠 관리**를 누릅니다.
3. 드래곤, 몬스터, 스킬, 아이템, 증강 탭을 선택합니다.
4. 왼쪽 목록에서 데이터를 선택하면 오른쪽에 편집 항목이 나타납니다.
5. 값을 바꾸면 에셋과 `ContentDatabase`에 저장됩니다.

위쪽 버튼의 기능:

- **새로 만들기:** 현재 탭의 빈 데이터를 생성합니다.
- **복제:** 선택한 데이터를 복사합니다. 비슷한 몬스터를 만들 때 가장 편합니다.
- **삭제:** 선택한 에셋을 삭제합니다. 출시 후 저장 데이터가 사용하는 ID는 삭제하지 마세요.
- **CSV 내보내기:** 현재 탭의 숫자와 이름을 CSV로 저장합니다.
- **CSV 가져오기:** ID를 기준으로 기존 데이터를 수정하고, 없는 ID는 새로 만듭니다.
- **전체 검사:** 중복 ID, 누락된 스킬, 잘못된 층 범위, 빠진 그림 등을 검사합니다.
- **DB 갱신:** `Assets/Data` 아래 에셋을 다시 찾아 실제 게임 데이터베이스에 등록합니다.

## 새 몬스터 추가 예시

1. 몬스터 탭에서 소형 골렘을 선택하고 **복제**합니다.
2. `Content Id`를 `monster_mud_golem`처럼 바꿉니다.
3. 표시 이름, 속성, HP, 공격력, 공격 간격을 설정합니다.
4. 등장 시작층·마지막층과 가중치를 설정합니다.
5. `Battle Sprite`에 투명 PNG 스프라이트를 끌어다 놓습니다.
6. **전체 검사**를 누릅니다.

`Spawn Weight`는 같은 층에 출현 가능한 몬스터끼리 비교하는 상대값입니다. 예를 들어 10, 10, 5라면 각각 약 40%, 40%, 20%입니다.

`HP Growth Per Floor`와 `Attack Growth Per Floor`는 등장 시작층 이후 층마다 더할 고정값입니다. `%` 항목은 그 결과에 추가로 곱해지는 층당 비율입니다. 모두 0이면 어느 층에서 만나도 입력한 기본 수치를 그대로 사용합니다.

## 고유 ID 규칙

ID는 영문 소문자, 숫자, 밑줄을 권장합니다.

```text
dragon_ember
monster_small_golem
skill_fire_burst
item_healing_potion
augment_quick_claw
```

화면에 표시되는 한글 이름은 바꿔도 되지만, 저장 데이터에서 사용하는 ID는 출시 후 바꾸지 마세요.

## CSV로 밸런스 수정

프로젝트의 `Balance` 폴더에 기본 CSV 양식이 들어 있습니다. Excel에서 한글이 깨지지 않도록 UTF-8 CSV로 저장하세요.

1. 콘텐츠 관리 창에서 원하는 탭을 선택합니다.
2. **CSV 내보내기**로 최신 파일을 만듭니다.
3. Excel이나 Google Sheets에서 숫자를 수정합니다.
4. CSV UTF-8 형식으로 저장합니다.
5. Unity에서 **CSV 가져오기**를 누릅니다.
6. **전체 검사**를 실행하고 Play로 확인합니다.

그림과 다른 에셋 연결은 CSV 가져오기로 지우지 않습니다. 새 CSV 행으로 생성한 콘텐츠의 그림은 Unity 콘텐츠 창에서 연결해야 합니다.

## 아이템과 증강 효과 작성법

Unity 편집창에서는 `Effects` 목록의 `+`를 눌러 효과를 여러 개 추가할 수 있습니다. CSV에서는 아래처럼 세미콜론으로 연결합니다.

```text
Heal:35
MaxHP:15;AttackDamage:2
SkillDamagePercent:20;SkillCooldownPercent:10
```

지원되는 효과:

| 효과 | 의미 |
|---|---|
| Heal | 현재 HP 회복 |
| MaxHP | 이번 도전의 최대 HP와 현재 HP 증가 |
| AttackDamage | 기본 공격력 증가 |
| SkillDamagePercent | 스킬 피해 비율 증가 |
| SkillCooldownPercent | 스킬 쿨타임 비율 감소 |
| AttackCooldownPercent | 기본 공격 쿨타임 비율 감소 |
| DodgeCooldownPercent | 회피 쿨타임 비율 감소 |
| DodgeDurationPercent | 회피 무적 시간 비율 증가 |
| Gold | 이번 도전의 골드 증가 |
| MaxHPPercent | 최대 HP 비율 증가·감소 |
| AttackDamagePercent | 일반 공격 피해 비율 증가·감소 |
| CriticalChancePercent | 치명타 확률 증가 |
| CriticalDamagePercent | 치명타 배율 증가 |
| SkillDisabled | 1이면 스킬 사용 봉인 |
| SkillCooldownSetZero | 1이면 스킬 쿨타임 제거 |
| DamageReductionPercent | 받는 피해 비율 감소 |

증강의 `Mechanic`은 적중·피격·회피·상태이상처럼 전투 중 발동하는 행동을 정합니다. `Primary Value`, `Secondary Value`, `Chance Percent`, `Duration`, `Trigger Count`에서 피해 비율·확률·지속시간·발동 횟수를 조정합니다. 이 값들은 증강 CSV에도 포함되므로 여러 증강을 한꺼번에 조정할 수 있습니다.

아이템은 `Kind`가 `Consumable`이면 전투 화면의 아이템 버튼으로 사용하고, `Equipment`이면 슬롯에 있는 동안 효과가 계속 적용됩니다. 서로 다른 아이템은 두 종류까지만 보유할 수 있으며 같은 아이템은 `Maximum Stacks`까지 한 슬롯에 중첩됩니다. 새 종류를 얻었을 때 두 슬롯이 차 있으면 교체할 슬롯을 고릅니다.

아이템 등급 등장 가중치는 일반 55, 레어 30, 에픽 12, 유니크 3입니다. 아이템방, 상점방, 몬스터 전리품 모두 이 등급 추첨을 사용합니다. 수치만 조절할 때는 아이템 CSV의 `grade`, `kind`, `maximumStacks`, `price`, `primaryValue`, `secondaryValue`, `duration`, `effects` 열을 수정하면 됩니다.

기존 효과와 Mechanic을 조합한 콘텐츠는 코드 수정 없이 추가할 수 있습니다. 완전히 새로운 발동 방식이 필요할 때만 새 Mechanic 코드가 필요합니다.

현재 임시 전투 기준은 기본 치명타 확률 0%, 치명타 피해 150%입니다. 사용자가 수치를 지정하지 않은 화염 잔재는 매초 공격력 20%로 3초, 찌릿찌릿은 적의 다음 공격을 1.5초 지연, 아이스크림은 3초간 공격속도 30% 둔화로 설정했습니다. 마나폭주와 뱀파이어의 HP 비용은 최대 HP 기준이며 HP를 1 아래로 내리지는 않습니다. 모두 증강 데이터에서 조정할 수 있습니다.

## 현재 자동 이전된 데이터

- 전투에 등록된 드래곤: 엠버, 루나, 제피르, 블랜디, 볼트, 옥타, 노바, 단테
- 몬스터: 1~9층 5종과 10층 고대 룬 골렘
- 아이템: 소모품과 장착 아이템 총 30종
- 스킬: 8속성별 4개씩 총 32종
- 증강: 일반·레어·에픽·유니크 총 43종

새 드래곤은 콘텐츠 창에서 계속 추가할 수 있지만 `Battle Sprite`와 스킬이 없으면 실제 알·도감 목록에 자동 등록되지 않습니다.
